# Core API dependencies only.
# The API runs in lexical retrieval mode by default and does not need
# numpy/faiss/sentence-transformers unless embedding mode is enabled.
# See requirements-ml.txt for that optional path and
# requirements-dashboard.txt for the Streamlit dashboard.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
google-genai
